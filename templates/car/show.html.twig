{% extends 'base.html.twig' %}

{% block title %}Car Show{% endblock %}

{% block body %}

    <div class="col-lg-12">
        <div class="card" style="border: solid; text-align: center; color: #fff;">
                <div class="header">
                    <h2>Marque : {{car.marque}}</h2>
                    <h2>Modele : {{car.modele}}</h2>
                    <h2>Categorie : {{car.categorie.nom}}</h2>
                    <h2>Kilometrage : {{car.kilometre}}</h2>
                    <h2>Energie : {{car.typeVehicule}}</h2>
                </div>
                <a style="color: #fff;" href="{{ path('formReservation',  {id: car.id}) }}"><button type="button" class="btn btn-primary">Réserver</a>
            </a>
        </div>
    </div>
{% endblock %}

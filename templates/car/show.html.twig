{% extends 'base.html.twig' %}

{% block title %}Car Show{% endblock %}

{% block body %}

    <div class="col-lg-4 all web">
        <div class="card">
            <div class="card-head">
                <div class="header">
                    <h2>{{car.marque}}</h2>
                    <p>{{car.modele}}</p>
                </div>
            </a>
            </div>
        </div>
    </div>
{% endblock %}
